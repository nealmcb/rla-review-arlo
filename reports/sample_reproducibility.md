---
layout: default
title: Sample Reproducibility
---

# Sample Reproducibility Analysis — Georgia May 19, 2026 RLA

## Summary

The Georgia May 19, 2026 RLA used Arlo (VotingWorks) BATCH_COMPARISON audit with MACRO math.
The public artifacts contain the random seed. Individual batch ticket numbers are
**fully reproducible** from public data. The complete MACRO sample draw (weighted
random selection) is **partially reproducible** but requires the exact Arlo/numpy version.

---

## Required Inputs for Full Sample Reproduction

| Input | Available? | Source |
|-------|------------|--------|
| Random seed | ✓ Yes | `final_audit_report.csv`, row 12: `06712221796172622814` |
| Ballot manifests | ✓ Yes | `manifests.zip` (downloaded) |
| Candidate totals per batch | ✓ Yes | `candidate_totals.zip` (downloaded) |
| consistent_sampler library | ✓ Yes | pip install consistent_sampler (public) |
| Contest totals (total ballots, candidates, winners) | ✓ Yes | `final_audit_report.csv` CONTESTS section |
| Risk limit | ✓ Yes | `final_audit_report.csv`: 5% |
| Arlo MACRO math source | ✓ Yes | `github.com/votingworks/arlo/server/audit_math/macro.py` |
| numpy version used at audit time | ✗ No | Not published |
| Arlo version / git commit used | ✗ No | Not published |
| Pre-seed committed manifests | ✗ No | Hash not verifiable from current files |

---

## Random Seed

The random seed is: **`06712221796172622814`**

This is a 20-digit number generated by rolling 20 ten-sided dice in a public event
on May 28, 2026. Each die contributes one digit (0–9), and the digits are concatenated.

The seed is embedded in the final audit report (Section: AUDIT SETTINGS, column "Random Seed").

---

## Ticket Number Verification

Arlo generates ticket numbers using the **Consistent Sampler** algorithm  
(`github.com/ron-rivest/consistent_sampler`):

```python
import consistent_sampler as cs

def batch_ticket(county: str, batch_name: str, seed: str) -> str:
    batch_key = (county, batch_name)
    ticket = cs.first_fraction(batch_key, seed)
    return cs.trim(ticket, 18)
```

### Verified Matches

All four ticket numbers sampled from the audit report were independently reproduced:

| County | Batch | Audit Report Ticket | Computed Ticket | Match |
|--------|-------|--------------------|--------------------|-------|
| BALDWIN | ED-North Baldwin ICP 1 - 0 | `0.427297706310130710` | `0.427297706310130710` | ✓ |
| BARROW | ICC-Absentee by Mail - 2 | `0.9088955580466120027` | `0.9088955580466120027` | ✓ |
| BARTOW | ED-Cartersville West ICP 1 - 0 | `0.468722862632969295` | `0.468722862632969295` | ✓ |
| WARE | AV-Administrative Building ICP 1 - 0 | `0.239288422106382693` | `0.239288422106382693` | ✓ |

This confirms that the seed in the audit report is genuine and that the Consistent Sampler
is being used as documented in Arlo's source code.

---

## MACRO Sample Draw Algorithm

For BATCH_COMPARISON/MACRO audits, Arlo does **not** draw the sample using simple
consistent-sampler ordering. Instead it uses **PPEB (Proportional with Error Bound)**
weighted random sampling:

```python
from numpy.random import default_rng
import consistent_sampler as cs

# Step 1: Convert seed to numpy seed
int_seed = int(cs.sha256_hex(seed), 16)
generator = default_rng(int_seed)

# Step 2: Compute MACRO error bounds per batch (from candidate totals + contest totals)
weighted_errors = [
    float(macro.compute_max_error(batch_results[batch], contest, unauditable) / U)
    for batch in batch_results
]

# Step 3: Draw sample_size batches with replacement, weighted by error bounds
sampled_batch_keys = generator.choice(
    list(batch_results.keys()),
    sample_size,
    p=weighted_errors,
    replace=True
)

# Step 4: Assign ticket numbers to each sampled batch
for batch_key in sampled_batch_keys:
    ticket = cs.first_fraction(batch_key, seed)
    ticket = cs.trim(ticket, 18)
```

### Key Points

1. The sample is drawn via `numpy.random.default_rng(int_seed).choice(...)` with PPEB weights
2. The weights are derived from per-batch MACRO error bounds (computable from the public candidate totals)
3. The numpy random number stream depends on the **exact numpy version** — different versions may produce different samples even with the same seed
4. All public inputs are available; only the numpy version is unknown

### MACRO Sample Sizes

From the audit report:
- **US Senate - Rep**: sample_size = 138 batches drawn (with replacement), resulting in 134 unique + 4 duplicates; 262 additional EXTRA batches combined into larger batches
- **Governor - Dem**: sample_size = 18 batches drawn

These are consistent with the small diluted margin for the Senate race (0.0222) requiring a larger sample, and the large margin for the Governor race (0.1952) requiring only 18 batches.

---

## Risk Calculation

The MACRO stopping rule computes a p-value (risk level) from:
1. The reported batch totals (from the candidate totals ZIP)
2. The hand-count audit results (in the final audit report, "Audit Results" columns)
3. The per-batch error bounds (the U and max_error values)

### Final Risk Levels

| Contest | Risk Limit | Achieved Risk (p-value) | Risk Met? |
|---------|------------|------------------------|-----------|
| US Senate - Rep | 5% | 0.049083729 (4.91%) | Yes |
| Governor - Dem | 5% | 0.041726014 (4.17%) | Yes |

Both contests passed the 5% risk limit.

### Risk Calculation Reproducibility

**Partially reproducible from public artifacts.** The final audit report provides:
- Reported results per batch (from candidate totals)
- Audit results per batch (hand counts)
- Change in results and change in margin per batch

However, the exact MACRO risk calculation also requires:
- The full batch_results structure with ballot counts per batch (from manifests)
- The Arlo MACRO source code (`macro.py`)
- The same floating-point arithmetic (Decimal precision)

The risk calculation **can** be replicated by downloading Arlo's `macro.py` and running it
against the public data. The required Arlo source is publicly available at
`github.com/votingworks/arlo`.

---

## Discrepancies Found

The audit found 100 batches with some difference between machine-reported and hand-counted results.
These are listed in the final audit report under "Change in Results" and "Change in Margin" columns.

Sample discrepancies (from audit report):
- WASHINGTON/AV-Elections Office ICP 1 - 0: Derek Dooley +1 in machine vs. hand count (margin change: 1)
- WAYNE/AV-Elections Office ICP 1 - 0: Carter +1, McColumn +1 (margin change: 2)
- WHITE/AV-Mauney Srvc Building ICP 2 - 0: Esteves +1, Jackson -1, Thurmond +1 (Dem margin change: 1)

These discrepancies are typical of the difference between QR-code machine reading and
human reading of the printed text on BMD ballots. The magnitude of discrepancies
(mostly 1-2 votes per batch, with net changes across all batches) is well within
the MACRO error bounds that allow the audit to conclude.

The fact that the p-value is 0.0491 (just below the 5% threshold) for the Senate race
indicates the audit was close to requiring an additional round — a sign of a tight race
and a meaningful audit rather than a rubber stamp.

---

## Conclusion

| Check | Result |
|-------|--------|
| Seed is public | ✓ Yes (in audit report) |
| Ticket numbers are reproducible | ✓ Yes (verified 4/4) |
| Full sample draw is reproducible | Partially — requires exact numpy version |
| Risk calculation is reproducible | Partially — requires Arlo macro.py |
| Risk limit was met | ✓ Yes (both contests) |
| Discrepancies explain outcome change | ✓ No — discrepancies are minor, no outcome change |

**The Georgia May 19, 2026 RLA is substantially reproducible from the public artifacts,  
with the caveat that the exact MACRO sample draw requires the numpy version used at audit time.**

The strongest reproducibility gap is that no official documentation specifies:
- Which Arlo version was used
- Which numpy version was used
- The sampler inputs file (contest totals, ballot totals, risk limit) as a structured artifact
